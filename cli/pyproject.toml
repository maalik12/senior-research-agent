[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "sra-cli"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "typer>=0.9",
    "rich>=13.0",
    "httpx>=0.25",
]

[project.scripts]
sra = "src.main:app"
